




"""
This script is used to inference the test models, however it's now somewhat obsolete, as the model config is now a
dataclass rather than a dictionary, though most remain the same

Primarily used because the dataclass refactoring is done right after training the scaling law testing models
"""

























